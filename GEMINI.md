# 프로젝트 필수 지침 (Project Mandates)
이 파일은 현재 작업 공간에서 Gemini CLI가 반드시 지켜야 할 기초적인 지침들을 포함하고 있습니다.

## 작업 환경 (Context)
- **프로젝트:** PlayableGames
- **설명:** `libs/vanilla.js`에 있는 커스텀 엔진으로 제작된 웹 기반 게임 모음 (오목, 과일 맞추기, 수박 게임 등).

## 개발 가이드라인 (Development Guidelines)
- `libs/vanilla.js`에 있는 기존의 코딩 스타일을 무조건 따르세요.
- 새로운 게임은 반드시 `main.js` 또는 메인 내비게이션에 통합되도록 하세요.
- `assets/` 안의 자산 구조를 일관되게 유지하세요.
- **스타일 유지:** 기존 코드 스타일과 주석 스타일을 임의로 변경하거나 삭제하지 마세요.
- **최소한의 수정:** 요청된 기능을 구현할 때 반드시 필요한 부분만 최소한으로 정확하게 수정하세요.
- **창작 금지:** 프로젝트 내에 존재하지 않는 가상의 함수나 클래스를 창작하여 사용하지 마세요. 반드시 제공된 코드베이스 내의 요소만 사용하세요.

## 필수 코딩 스타일 지침 (CRITICAL)
- **변수명 유지 및 축약 금지:** 기존 코드의 변수명을 절대로 임의로 변경하지 마세요. 또한 변수명을 줄여 쓰거나(축약) 약어를 사용하는 것을 엄격히 금지합니다. (예: `canvasRenderingContext`를 `ctx`로 축약 금지)
- **지역 변수 할당 강제:** `get` 메서드나 프로퍼티의 반환값을 다른 함수의 인자로 직접 전달하지 마세요. 반드시 축약하지 않은 풀네임의 지역 변수에 먼저 할당한 후, 해당 지역 변수를 함수 인자로 사용하거나 그 변수를 통해 다른 함수를 호출해야 합니다.
- **다중 구문 작성 금지:** 한 줄에 세미콜론(`;`)을 하나 초과하여 사용하지 마세요. (예: `a = 1; b = 2;` 형식 금지, 각 구문은 반드시 새로운 줄에 작성)
- **중괄호 강제:** 모든 조건문, 반복문, 함수(`if`, `else`, `for`, `while`, `function` 등)는 반드시 중괄호 `{}`를 사용해야 하며, 1줄이라도 생략을 금지합니다. 여는 중괄호 `{`는 반드시 시작행의 끝에 위치해야 하며, 닫는 중괄호 `}`는 반드시 새로운 행에 위치해야 합니다.
- **조건문 개행 강제:** `else`, `else if`, `catch`, `finally` 등 닫는 중괄호 `}` 이후에 이어지는 구문은 반드시 다음 행에서 시작해야 합니다. (예: `}\nelse {`)
- **switch 문 중괄호 강제:** `switch` 문의 모든 `case`와 `default` 블록은 반드시 중괄호 `{}`를 사용해야 합니다. 또한, `break;` 문은 반드시 해당 중괄호 블록 `{}` 내부에 위치해야 합니다. (예: `case value: { ... break; }`)
- **Private 멤버 강제 지침:** 모든 클래스의 `private` 멤버 변수 및 메서드는 반드시 `#` 접두사를 사용해야 합니다. (예: `this.#privateVariable`) 기존 코드에서 `_`로 시작하는 멤버를 발견하면 적극적으로 `#`으로 교체하세요.
- **주석 및 기존 스타일 보존:** 기존 코드의 JSDoc, 함수 헤더 주석(`//=================`), 인라인 주석 등 모든 형태의 주석과 기존 코딩 스타일을 절대 삭제하거나 임의로 수정하지 마세요.

## 소통 가이드라인 (Communication Guidelines)
- **언어:** 항상 모든 응답과 설명을 한국어로만 작성하세요.
- **코드 주석:** 코드를 수정할 때, '무엇을 변경했다'는 식의 불필요한 변경 이력 주석(예: `// 틈새를 메우기 위해...`)을 코드 내에 절대 추가하지 마세요.
